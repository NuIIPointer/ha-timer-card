name: Validate

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 12 * * *"

jobs:
  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master
